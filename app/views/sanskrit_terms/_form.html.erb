<%= form_with(model: sanskrit_term, local: true) do |form| %>
  <% if sanskrit_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sanskrit_term.errors.count, "error") %> prohibited this sanskrit_term from being saved:</h2>

      <ul>
      <% sanskrit_term.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :term %>
    <%= form.text_field :term, id: :sanskrit_term_term %>
  </div>

  <div class="field">
    <%= form.label :definition %>
    <%= form.text_area :definition, id: :sanskrit_term_definition %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
